import { css } from "@emotion/react";

export const container = css({
  borderRadius: "5px",
  padding: "8px",
  backgroundColor: "#fff",
});

export const heading = css({
  display: "block",
  marginBlock: "4px",
  fontSize: "1rem",
});
